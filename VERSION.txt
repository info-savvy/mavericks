Mavericks Website — Build v6  (20260602-1213)
=============================================
About / The Club page — MOBILE FIX:
Root cause: the lounge image-grid styling (grid layout, borders, image
sizing AND the zoom transition) was inside the desktop @media block, so on
mobile the lounge images had no grid, no borders, no spacing, and no
transition (instant = "harsh" zoom). The Main Floor was unaffected because
its rules are mobile-first.

Fix: moved the lounge-grid rules to mobile-first base styles —
  - Mobile: full-width single-column images, matching borders/rounded corners,
    consistent 14px spacing, and the same .6s soft zoom as the Main Floor.
  - Desktop: 2-across grid retained (unchanged look).

Now all lounge images behave identically to the Main Floor on both mobile
and desktop.
