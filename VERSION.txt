Mavericks Website — Build v4  (20260602-1152)
=============================================
Change in this build:
- Tap-to-zoom now works on ALL gallery/feature images on touch devices,
  matching the Main Floor behaviour. Tap an image to zoom it slightly; tap
  again (or tap another image) to release. Desktop continues to zoom on hover.
  Applied site-wide across all 7 pages.

Includes all prior fixes (v2: nav submenus, add-on prices, Group Experiences;
v3: lounge image borders, hover zoom, consistent spacing).
